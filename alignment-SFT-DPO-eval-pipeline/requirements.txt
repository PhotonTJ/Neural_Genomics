# =============================================================================
# UNIFIED EVALUATION PIPELINE — Requirements
# =============================================================================
# Install: pip install -r requirements.txt
# 
# Note: Install PyTorch FIRST if not already present, matching your CUDA version.
#   See: https://pytorch.org/get-started/locally/
#   Example: pip install torch --index-url https://download.pytorch.org/whl/cu121
# =============================================================================

# ── Core ML / Serving ────────────────────────────────────────────────────────
vllm                        # Model serving (OpenAI-compatible API)
torch                       # PyTorch (install matching your CUDA version)
transformers                # HuggingFace model loading
sentence-transformers       # Embedding models (used by DetoxifyCalculator internals)

# ── Evaluation Framework ─────────────────────────────────────────────────────
deepeval                    # GEval metrics, LLM-as-judge framework
openai                      # OpenAI-compatible client (for vLLM connection)
detoxify                    # Standalone toxicity scoring

# ── Data & Benchmarks ────────────────────────────────────────────────────────
datasets                    # HuggingFace datasets (benchmark downloads)
huggingface_hub             # HF authentication & model downloads

# ── Data Processing ──────────────────────────────────────────────────────────
pandas                      # DataFrames, CSV handling
numpy                       # Numerical operations
pydantic                    # Schema validation (used by DeepEval)

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib                  # Plotting
seaborn                     # Enhanced plot styling

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm                        # Progress bars

# ── Optional: IFEval programmatic verification ───────────────────────────────
# Uncomment to enable full IFEval constraint checking (not just heuristic):
# ifeval @ git+https://github.com/oKatanaaa/ifeval.git
